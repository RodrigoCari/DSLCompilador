CONNECT("broker.hivemq.com", 1883)

SET MOTIONSENSOR

TOPIC MOTION_STATE ("/indobot/p/pir/state")
TOPIC MOTION_STATUS ("/indobot/p/pir/status")

TIMER PUBLISH_LIGHT (1000) {
    PUBLISH(MOTION_STATE, MOTIONSENSOR.STATE)
    PUBLISH(MOTION_STATUS, MOTIONSENSOR.STATUS)

    PRINT("STATE: " + MOTIONSENSOR.STATE + " --> " + MOTIONSENSOR.STATUS)
}